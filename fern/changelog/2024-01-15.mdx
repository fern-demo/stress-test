## New plant categories

Added support for three new plant categories: `herb`, `flowering`, and `tropical`. These categories are now available in the Plant API's search and create endpoints.

### What changed

- The `category` field on the `Plant` schema now accepts `herb`, `flowering`, and `tropical` in addition to the existing `fern` and `succulent` values.
- Search filters have been updated to include the new categories.
